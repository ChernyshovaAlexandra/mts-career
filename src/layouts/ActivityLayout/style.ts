import { mts_bg_lower } from "@chernyshovaalexandra/mtsui";
import styled from "styled-components";

export const GrayBlock = styled.div`
  border-radius: 32px;
  background: ${mts_bg_lower};
  padding: 30px;
`;
